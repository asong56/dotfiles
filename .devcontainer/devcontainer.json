{
  "name": "dotfiles",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",

  "postCreateCommand": "bash -lc 'sudo apt-get update -qq && sudo apt-get install -y stow zsh git && ./install.sh'",

  "remoteEnv": {
    "DOTFILES": "${containerWorkspaceFolder}"
  }
}
